#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#include<stdlib.h>
#include<assert.h>


void InsertSortIndex(int** a, int n)
{
	for (int i = 0; i < n + 1; i++)
	{
		int end = i;
		int tmp = end + 1;
		while (end >= 0)
		{
			if()
		}
	}
}


void QuickSortIndex(int** a, int left, int right)
{
	if (left >= right)
	{
		return;
	}
	if (right - left + 1 < 10)
	{
		InsertSort(a + left, right - left + 1);
	}
}


int maxCoins(int* nums, int numsSize) {

}


int main()
{

}